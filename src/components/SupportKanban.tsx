import React from 'react';
import { Box, Typography } from '@mui/material';

const SupportKanban: React.FC = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Tablero Kanban (En Construcción)</Typography>
        </Box>
    );
};

export default SupportKanban;
